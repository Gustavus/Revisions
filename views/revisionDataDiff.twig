{% extends 'revisionData.twig' %}
{% import 'baseTable.twig' as baseTable %}

{% block headers %}
  <th>Field</th>
  <th>Diff</th>
{% endblock %}

{% block rows %}
  <td>{{ key }}</td>
  {% if revisionData.error %}
    {{ baseTable.renderErrorColumns(1) }}
  {% else %}
    <td>{{ revisionData.makeRevisionContent(true)|raw }}</td>
  {% endif %}
{% endblock %}